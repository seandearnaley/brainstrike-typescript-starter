type Category {
  id: ID
  name: String
  parentId: ID
  children: [Category]
  cards: [Card]
}

input CategoryInput {
  name: String
}

type CategoryUpdatedResponse {
  success: Boolean!
  message: String!
  category: Category
}

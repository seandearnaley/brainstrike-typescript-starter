type Category {
  id: ID!
  name: String
  parentId: ID
  children: [Category]
  updated: DateTime
  created: DateTime
}

input CategoryInput {
  name: String
}

type CategoryUpdatedResponse {
  success: Boolean!
  message: String!
  category: Category
}

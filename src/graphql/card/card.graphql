type Card {
  id: ID!
  number: Int
  label: String
  description: String
  created: DateTime
  updated: DateTime
}

input CardInput {
  number: Int
  label: String
  description: String
}

type CardsUpdatedResponse {
  success: Boolean!
  message: String
  card: Card
}

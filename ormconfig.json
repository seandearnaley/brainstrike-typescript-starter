{
   "type": "postgres",
   "host": "localhost",
   "port": 5432,
   "database": "brainstrike",
   "synchronize": true,
   "logging": false,
   "migrations": [
      "src/migration/**/*.ts"
   ],
   "entities": [
      "src/entity/**/*.ts"
   ]
}
type Category {
  id: ID!
  name: String
  parentId: ID
  children: [Category]
}

input CategoryInput {
  name: String
}

type CategoryUpdatedResponse {
  success: Boolean!
  message: String!
  category: Category
}

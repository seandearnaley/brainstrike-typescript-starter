import React from 'react';
import { useStyles } from '../styles';

export const CardsNav: React.FC = (): React.ReactElement => {
  const classes = useStyles();

  return <div className={classes.navRoot}>WIP</div>;
};

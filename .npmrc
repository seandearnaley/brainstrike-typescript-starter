# pnpm workspace configuration

# Selective hoisting for better performance and compatibility
hoist=false
public-hoist-pattern[]=@apollo/*
public-hoist-pattern[]=@types/*

# Auto install peer dependencies
auto-install-peers=true

# Strict peer dependencies
strict-peer-dependencies=false

# Use hard links for better performance
package-import-method=hardlink

# Disable shameless hoisting for better isolation
shamefully-hoist=false

# Link workspace packages by default
link-workspace-packages=true

# Save workspace protocol
save-workspace-protocol=rolling

# Recursive install
recursive-install=true

# Enable lockfile validation
prefer-frozen-lockfile=true 